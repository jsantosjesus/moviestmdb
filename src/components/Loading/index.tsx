import { CircularProgress } from "@mui/material";

import './loading.sass'

const Loading = () => {
    return (
        <div id="div-loading">
           <CircularProgress /> 
        </div>
    );
}
export default Loading;
